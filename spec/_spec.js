const with_cases = require('./spec_helper').with_cases;

const chai = require('chai');
const expect = chai.expect;
chai.should();

//require('../src/');

describe('something', () => {

    it('does something', () => {
        //
    });

});
