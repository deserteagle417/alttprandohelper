{
  "name": "alttprandohelper",
  "private": true,
  "scripts": {
    "build": "babel src -d js",
    "watch": "babel src -d js --watch",
    "test": "mocha --recursive ./spec/*_spec.js"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "lodash": "^4.17.11",
    "chai": "^4.2.0"
  },
  "babel": {
    "presets": [
      ["env", { "modules": false }],
      "stage-2",
      "react"
    ]
  }
}
